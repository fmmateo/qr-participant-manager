
export interface Program {
  id: string;
  name: string;
  type: string;
}

export interface Template {
  id: string;
  name: string;
  template_url: string;
}

export interface Participant {
  id: string;
  name: string;
  email: string;
}
